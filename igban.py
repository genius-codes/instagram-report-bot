import requests

def login(username, password):
    # (igservicemaster,shiru@@12)

def report_user(session, user_id):
    # (its.still.alex)

if __name__ == "__main__":
    # Replace 'your_username' and 'your_password' with your actual Instagram credentials
    your_username = 'igservicemaster'
    your_password = 'shiru@@12'
    
    user_id_to_report = 'its.still.alex'  # Replace '123456789' with the user ID of the profile you want to report
    num_reports = 1000

    session = login(igservicemaster,shiru@@12)

    for i in range(num_reports):1000
        report_user(session,its.still.alex)
        print(f"Report {i + 1} completed.")

    print("All reports completed.")
